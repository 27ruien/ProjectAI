import { cn, formatDate } from "@/lib/utils";

export type ActivityLike = { id: string; title?: string; description?: string; type?: string; createdAt?: string; timestamp?: string; actor?: string };
export function ActivityTimeline({ items }: { items: ActivityLike[] }) { return <ol className="space-y-0">{items.map((item, index) => <li key={item.id} className="relative flex gap-3 pb-5 last:pb-0">{index < items.length - 1 ? <span className="absolute left-[5px] top-3 h-full w-px bg-border" /> : null}<span className={cn("relative mt-1.5 size-[11px] shrink-0 rounded-full border-2 border-card", item.type === "risk" ? "bg-destructive" : item.type === "approved" ? "bg-success" : "bg-primary")} /><div className="min-w-0 flex-1"><p className="text-xs font-medium">{item.title ?? item.description}</p>{item.title && item.description ? <p className="mt-0.5 text-xs text-muted-foreground">{item.description}</p> : null}<p className="mt-1 text-[10px] text-muted-foreground">{item.actor ? `${item.actor} · ` : ""}{formatDate(item.createdAt ?? item.timestamp ?? "", true)}</p></div></li>)}</ol>; }
