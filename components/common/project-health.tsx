import { cn } from "@/lib/utils";

const health = { normal: { label: "正常", color: "bg-success", text: "进度、Scope 与关键风险均在可控范围" }, attention: { label: "关注", color: "bg-warning", text: "存在临近延期或未确认事项，建议本周处理" }, risk: { label: "风险", color: "bg-destructive", text: "关键里程碑或依赖已受影响，需要项目负责人介入" }, severe: { label: "严重风险", color: "bg-destructive", text: "上线目标已受到直接影响，需要立即决策" } } as const;
export function ProjectHealth({ level = "attention", score = 72, compact = false }: { level?: keyof typeof health; score?: number; compact?: boolean }) { const item = health[level]; return <div className={cn("rounded-lg border bg-card", compact ? "p-3" : "p-4")}><div className="flex items-center justify-between"><span className="text-xs font-medium text-muted-foreground">项目健康度</span><strong className="text-sm">{item.label}</strong></div><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-muted"><div className={cn("h-full rounded-full", item.color)} style={{ width: `${score}%` }} /></div>{!compact ? <p className="mt-2 text-xs leading-5 text-muted-foreground">{item.text}</p> : null}</div>; }
