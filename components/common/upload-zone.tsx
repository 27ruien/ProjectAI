"use client";
import { UploadCloud } from "lucide-react";
import { useRef } from "react";
import { useToast } from "./toast";
export function UploadZone({ onFiles }: { onFiles?: (files: File[]) => void }) { const inputRef = useRef<HTMLInputElement>(null); const { toast } = useToast(); const handle = (files: FileList | null) => { if (!files?.length) return; onFiles?.(Array.from(files)); toast(`已接收 ${files.length} 个文件，正在进入 Mock 解析队列`); }; return <button onClick={() => inputRef.current?.click()} onDragOver={(event) => event.preventDefault()} onDrop={(event) => { event.preventDefault(); handle(event.dataTransfer.files); }} className="flex w-full flex-col items-center justify-center rounded-xl border border-dashed bg-surface p-8 text-center transition-colors hover:border-primary/45 hover:bg-primary/[0.025]"><input ref={inputRef} type="file" multiple className="hidden" onChange={(event) => handle(event.target.files)} /><span className="grid size-10 place-items-center rounded-xl bg-primary/10 text-primary"><UploadCloud className="size-5" /></span><strong className="mt-3 text-sm">拖放或点击上传项目资料</strong><span className="mt-1 text-xs text-muted-foreground">支持 PDF、Word、Excel、邮件与图片，单文件最多 50MB</span></button>; }
